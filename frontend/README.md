# frontend

React reader UI (PDF view, highlights, side-by-side, chat panel). Vite + React, minimal starter.

## Run

```bash
npm install
npm run dev
```

Opens on http://localhost:5173. Proxies `/api/*` to the backend on port 8000.
